{% set name = "mne-qt-browser" %}
{% set version = "0.7.5" %}
{% set sha256 = "2a7fcf892f9fa31b8a678f600d946dc3090d2c1576aa3643851f0c97d98dd269" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  noarch: python

requirements:
  build:
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
  host:
    - pip
    - python {{ python_min }}
    - hatchling
    - hatch-vcs
  run:
    - python >={{ python_min }}
    - numpy
    - scipy
    - matplotlib-base
    - qtpy
    - scooby
    - packaging
    - mne-base >=1.0
    - pyqtgraph >=0.12.3
    - darkdetect
    - qdarkstyle

test:
  imports:
    - mne_qt_browser
  commands:
    - pip check
    - python -c "import mne_qt_browser; ver = mne_qt_browser.__version__; assert ver == '{{ version }}', ver"
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/mne-tools/mne-qt-browser
  summary: A new backend based on pyqtgraph for the 2D-Data-Browser in MNE-Python.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - hoechenberger
    - larsoner
    - marsipu
